% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_valresultat.R
\name{valresultat}
\alias{valresultat}
\title{Officiella valresultat för 2026}
\usage{
valresultat(
  ar = 2026,
  val = "RD",
  rakning = c("slutlig", "preliminar"),
  niva = NULL,
  source = c("auto", "local", "remote"),
  data_dir = NULL,
  update = FALSE,
  archive = FALSE,
  progress = interactive()
)
}
\arguments{
\item{ar}{Valår. Endast 2026 stöds.}

\item{val}{Exakt en valtyp: \code{"RD"}, \code{"RF"} eller \code{"KF"}. Inte \code{NULL}.}

\item{rakning}{Exakt en räkning: \code{"slutlig"} (default) eller \code{"preliminar"}.}

\item{niva}{En geografisk nivå. \code{NULL} ger valets huvudnivå: RD \code{"riket"},
RF \code{"region"}, KF \code{"kommun"}. Se nivåerna nedan.}

\item{source}{Datakälla: \code{"auto"}, \code{"local"} eller \code{"remote"}.}

\item{data_dir}{Lokal rotmapp för rådata.}

\item{update}{Om \code{TRUE}, uppdateras lokala arbetskopior.}

\item{archive}{Om \code{TRUE}, sparas även daterade snapshots.}

\item{progress}{Visa progressindikator.}
}
\value{
En tibble med 83 kolumner, en geografisk nivå och unika
val-/områdes-/partinycklar. Inga mandat eller personröster ingår.
}
\description{
Läser en valtyp, en räkning och en geografisk nivå från Valmyndighetens
officiella primärkälla. Ingen geografisk aggregering eller automatisk
reservkälla används om den valda källan saknas.
}
\details{
RD stöder valdistrikt, kommun, kommunvalkrets, riksdagsvalkrets och riket.
RF stöder valdistrikt, kommun, kommunvalkrets, region, regionvalkrets och
riket. KF stöder valdistrikt, kommun, kommunvalkrets, lan och riket.
RD/lan stöds inte. RF/lan är dokumenterat i OS-formatet men ännu inte
aktiverat, eftersom faktisk slutlig källa inte är verifierad.

Distriktsresultat läses ur individuell röstfördelning. RD/RF:s kommuner
och kommunvalkretsar läses ur underordnad summering. Valområden och deras
mandatvalkretsar läses ur mandatfilens röstfördelning. RF/KF:s riksresultat
och KF:s länsresultat läses ur överordnad summering (OS), om filen finns
i valt index. Räkningstillfället bevaras i \code{rakningstillfalle}.

En rad avser val, geografiskt område och parti/kategori. Områdestotaler
upprepas på partiraderna och ska inte summeras över partier. Samtliga
källor ger samma 83 kolumner. Datum, tidsstämplar och geografiska koder
är character, antal integer, andelar double i procent och indikatorer
logical. Saknade eller ej tillämpliga fält är typade \code{NA}.
Historik och differenser bevaras endast där källan publicerar dem.

\code{antal_valdistrikt_raknade} och \code{antal_valdistrikt_som_ska_raknas} avser
filpopulationen; motsvarigheterna med suffix \verb{_omrade} avser radens område
och är \code{NA} för valdistrikt. Distriktens \code{valdel} kommer från
\code{valdeltagandeVallokal}, övriga nivåers från \code{valdeltagande}.
\code{over_sparr} fylls bara från ett uttryckligt relevant besked i mandatkällan.
Övriga partier får en rad endast om källnoden finns; explicit noll behålls.
Verifierat tom valkretsindelning ger en typad tom tabell, medan saknad
röstfördelning för ett existerande område ger fel.

\code{data_dir} går före optionen \code{valresultat.data_dir}. Resultatsamlingen
väljs med optionen \code{valresultat.resultatsamling_2026} (default \code{"val2026"}).
Test-/utvecklingssamlingen \code{"genrep2026"} kan väljas uttryckligen.
\code{source = "local"} använder aldrig nätet. Lokal arkivering kopierar endast
befintliga filer; en snapshot från samma datum får ersättas.
}
\examples{
\dontrun{
valresultat(val = "RD", source = "local", data_dir = "mitt_arkiv")
valresultat(val = "KF", niva = "kommun", rakning = "preliminar")
}
}
\seealso{
\code{\link[=mandat]{mandat()}}, \link{valresultat-package}
}
